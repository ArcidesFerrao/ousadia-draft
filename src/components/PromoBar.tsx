import React from "react";

export const PromoBar = () =>
  // { message, link }
  {
    return (
      <div>
        {/* <a href={link}>
            {message}
        </a> */}
      </div>
    );
  };
